# ZeqouXTraining — Python ML runtime
#
# NVIDIA + CUDA is the primary, first-class target. These packages are only
# needed for the *training* and *inference* stages. Dataset validation,
# environment checks and hardware detection work on bare Python (stdlib only),
# so the app can always diagnose a machine even when PyTorch is missing.
#
# For NVIDIA GPUs prefer installing torch from the CUDA wheel index, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# The app's Settings → Environment screen generates the exact command.

torch>=2.4
transformers>=4.44
peft>=0.13
accelerate>=1.0
safetensors>=0.4
huggingface_hub>=0.25
numpy>=1.24

# Quantization backend for QLoRA / 8-bit. NVIDIA only in practice.
bitsandbytes>=0.44; platform_system == "Windows" or platform_system == "Linux"

# Hugging Face datasets, plus parquet support for local dataset folders.
datasets>=3.0
pyarrow>=16

# Tokenizer back-ends used by several popular model families.
sentencepiece>=0.2
protobuf>=5
